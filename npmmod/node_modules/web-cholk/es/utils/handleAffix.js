import appendStyle from './appendStyle.js';
import applyParams from './applyParams.js';
import handleStyleValue from './handleStyleValue.js';
import isTypeof from './isTypeof.js';

function handleAffix(affix = {}) {
    const target = {
        _template: '',
        _params: [],
    };
    affix?.css && appendStyle(target, handleStyleValue(affix?.css));
    affix?.log &&
        applyParams(target, isTypeof(affix?.log, 'Array') ? affix?.log : [affix?.log]);
    return target;
}

export { handleAffix as default };
