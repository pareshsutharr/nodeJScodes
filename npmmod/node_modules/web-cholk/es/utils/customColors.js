function customColors(STYLE, colors) {
    for (let key in colors) {
        const value = colors[key];
        STYLE[key] = `color: ${value};`;
        STYLE[`bg${key.slice(0, 1).toUpperCase()}${key.slice(1)}`] = `background-color: ${value};`;
    }
}

export { customColors as default };
