function customStyles(STYLE, styles) {
    for (let key in styles) {
        let value = `${styles[key]}`;
        if (!value.endsWith(';')) {
            value += ';';
        }
        STYLE[key] = value;
    }
}

export { customStyles as default };
