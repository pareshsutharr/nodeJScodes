import appendStyle from './appendStyle.js';
import handleLogValue from './handleLogValue.js';
import handleStyleValue from './handleStyleValue.js';

function handlePresetCall(target, preset) {
    const { css, log } = preset;
    appendStyle(target, handleStyleValue(css));
    handleLogValue(target, log);
}

export { handlePresetCall as default };
